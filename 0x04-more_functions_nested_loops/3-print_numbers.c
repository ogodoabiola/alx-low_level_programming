#include "main.h"

/**
 * print_numbers - primary fn
 * Return: 0
 */

void print_numbers(void)
{
for (c >= 48; c <= 57; c++)
char c = c + '0';
for (c >= 48; c <= 57; c++)
_putchar(c);
_putchar('\n');
return (0);
}
